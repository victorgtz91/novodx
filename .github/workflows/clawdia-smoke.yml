name: clawdia-smoke
on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "clawdia github skill smoke ok"
